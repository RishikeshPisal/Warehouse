from django.db import models
from customers.models import Customer
from crops.models import Crop
# Create your models here.

# class Rent(models.Model):
#   entry = models.ForeignKey(Customer,on_delete=models.CASCADE)


